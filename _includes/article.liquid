<article>
  <div class="columns is-vbottom">
    <div>
      <h2 class="article-title">
        <a href="{{ url }}">{{ title }}</a>
      </h2>
      <h4 class="article-author">by Stan Lemon</h4>
    </div>
    <h3 class="article-date">{{ date | date: "%b %d, %Y" }}</h3>
  </div>

  <hr>

  {{ content }}

  {% if keepReading %}
    <div class="keep-reading">
      <a href="{{ site.url }}{{ url }}">Keep Reading</a>
    </div>
  {% endif %}

  <hr>
</article>
